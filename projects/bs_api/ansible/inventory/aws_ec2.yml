---
plugin: amazon.aws.aws_ec2
regions:
  - us-west-1
filters:
  tag:Name:
    - buildserver
  tag:Env:
    - "{{ lookup('env', 'ENV') | default('prod') }}"
  instance-state-name: running
hostnames:
  - tag:Name
compose:
  ansible_host: instance_id
